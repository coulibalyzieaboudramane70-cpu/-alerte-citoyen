"use client";
import {useState} from "react"; import Link from "next/link";
export default function Create(){const [msg,setMsg]=useState("");async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const raw=Object.fromEntries(f);const data=Object.fromEntries(Object.entries(raw).filter(([,v])=>v!==""));const r=await fetch("/api/alerts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();if(r.ok)location.href="/alerts";else setMsg(j.error||"Erreur");}return <main className="section"><div className="container" style={{maxWidth:820}}><div className="card"><h1>Créer une alerte</h1><div className="notice">🛡️ Ne publiez pas d'adresse privée, numéro de téléphone, pièce d'identité ou autre donnée sensible si ce n'est pas nécessaire.</div><form onSubmit={submit} style={{marginTop:18}}><div className="formGrid"><div className="field"><label>Type</label><select name="type"><option value="MISSING_PERSON">Personne disparue</option><option value="LOST_ITEM">Objet perdu</option><option value="LOST_DOCUMENT">Document perdu</option><option value="DANGEROUS_SITUATION">Situation dangereuse</option><option value="LOST_ANIMAL">Animal perdu</option><option value="OTHER">Autre</option></select></div><div className="field"><label>Visibilité</label><select name="visibility"><option value="PUBLIC">Public</option><option value="LIMITED">Zone limitée</option><option value="PRIVATE">Privé</option><option value="ANONYMOUS">Anonyme</option></select></div><div className="field full"><label>Titre</label><input name="title" required/></div><div className="field full"><label>Description</label><textarea name="description" required/></div><div className="field"><label>Ville</label><input name="city"/></div><div className="field"><label>Région</label><input name="region"/></div><div className="field"><label>Nom de la personne (si applicable)</label><input name="personName" placeholder="Utilisé pour le rapprochement automatique"/></div><div className="field"><label>Âge approximatif</label><input name="personAge" type="number" min={0} max={120}/></div></div><label><input type="checkbox" required/> Je confirme avoir le droit de publier ces informations et j'accepte la <Link href="/privacy">politique de confidentialité</Link>.</label><br/><br/><button className="btn primary">Soumettre à la modération</button>{msg&&<p className="notice">{msg}</p>}</form></div></div></main>}
