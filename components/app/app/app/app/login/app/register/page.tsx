"use client";
import {useState} from "react"; import Link from "next/link";
export default function Register(){const [msg,setMsg]=useState("");async function submit(e:any){e.preventDefault();setMsg("Création du compte en cours...");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const j=await r.json();setMsg(j.message||j.error);if(r.ok)location.href="/dashboard"}return <main className="section"><div className="container" style={{maxWidth:620}}><div className="card"><h1>Créer mon compte</h1><p className="muted">Un compte est nécessaire pour publier et contribuer.</p><form onSubmit={submit}><div className="field"><label>Nom</label><input name="name" required minLength={2}/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Mot de passe</label><input name="password" type="password" minLength={10} required/><small className="muted">10 caractères minimum.</small></div><label><input type="checkbox" required/> J'accepte les <Link href="/terms">conditions</Link> et la <Link href="/privacy">politique de confidentialité</Link>.</label><br/><br/><button className="btn primary">Créer le compte</button>{msg&&<p className="notice">{msg}</p>}</form></div></div></main>}
