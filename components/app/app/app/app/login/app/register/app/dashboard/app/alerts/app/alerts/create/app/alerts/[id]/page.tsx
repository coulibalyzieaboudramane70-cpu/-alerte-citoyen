import {db} from "@/lib/db"; import Link from "next/link"; import {notFound} from "next/navigation";
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;const a=await db.alert.findUnique({where:{id}});if(!a)notFound();return <main className="section"><div className="container" style={{maxWidth:850}}><Link href="/alerts">← Alertes</Link><div className="card" style={{marginTop:15}}><span className="badge green">{a.status}</span><h1>{a.title}</h1><p className="muted">{a.city||"Zone non précisée"} • {a.country}</p><div className="notice">🔐 Les données de contact privées de l'auteur ne sont jamais affichées publiquement.</div><h3>Description</h3><p style={{whiteSpace:"pre-wrap",lineHeight:1.7}}>{a.description}</p><p className="muted">Référence : {a.reference}</p></div></div></main>}
